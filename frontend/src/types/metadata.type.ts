export interface UserGlobalMetadata {
  totalCreatedTasks: number;
  totalAssignedTasks: number;
  totalOverdueTasks: number;
}

export interface UserNotificationsMetadata {
  totalUnread: number;
  total: number;
}
